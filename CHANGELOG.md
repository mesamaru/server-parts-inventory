# 変更履歴

バージョンは `メジャー.マイナー.バグ修正` の3つで管理します。

| 位置 | 上げるとき |
| --- | --- |
| メジャー | 互換性が無くなる変更（データの持ち方が変わる、既存の使い方が通らなくなる等） |
| マイナー | 機能追加 |
| バグ修正 | 不具合の修正のみ |

バージョンの実体は `package.json` の `version` です。画面右上とログイン画面に表示され、
`GET /api/version` でも取得できます。

> npm は先頭が0の数字（`01` など）を不正なバージョンとして扱うため、`1.01.00` ではなく
> `1.1.0` のように0埋めなしで記載しています。順序の意味は上表のとおりです。

## 1.3.0

### 追加

- サーバー一覧に「簡易表示／詳細表示」の切り替えを追加。詳細表示ではCPU（名称・コア数/スレッド数・
  動作クロック）、メモリ（合計容量と内訳、例: `96GB（内訳 16, 16, 32, 32）`）、ストレージ構成を、
  現在の割り当てパーツから自動集計して一覧できる
- 構成同期を、送信時にサーバーを指定しなくても受け付けるように変更。検出スクリプトはAPIトークンを
  実行時に入力する形（プロンプト）がデフォルトになり、コマンドに埋め込む必要がなくなった
  （`--server` はコマンド生成からも削除）
- 届いた構成データがどのサーバーのものか未指定の場合は「未割り当てのレポート」として保持し、
  管理画面でパーツを一括選択してから対象サーバーを選ぶ（その場で新規サーバー登録も可能）形に変更した
- 構成同期の差分（取り外す／割り当てる／未登録）に、グループ単位の「全て選択」チェックボックスを
  追加。特に未登録グループは一括選択・一括除外がしやすくなった

### 修正

- サーバー編集のステータス欄が自由入力（テキスト入力＋候補）だったのを、プルダウン選択のみに変更。
  既存データが候補にない値の場合は「（旧設定）」として選択肢に残し、上書き保存するまでは消えない

## 1.2.0

### 追加

- サーバーの「高度な設定」からSSH接続情報を登録し、ボタン一つで実機の構成を取得できるように
  した。対象へSFTPで検出スクリプトを転送してその場で実行し、結果を構成同期の差分として保存する
  （実行後は転送したスクリプトを削除する）。パスワード/秘密鍵は `data/secret.key` を鍵にAES-256-GCM
  で暗号化して保存し、APIやUIに平文で返すことはない
- 構成同期の生成コマンドで sudo の有無を切り替えられるように（チェックボックス）
- ダークモードに対応（ライト／ダーク／端末の設定に合わせる の3択、設定タブから切り替え）
- スマホ幅でのレイアウトを整理。タブはハンバーガーメニューに収まり、ダイアログは全画面表示になる
- カテゴリ表記の設定を、画面上部から「設定」タブの表示設定にまとめた

### 修正

- 構成同期で新しいAPIトークンを選んでも、コピー操作でクリップボード書き込みに失敗すると
  発行されたかどうか分かりにくかった。トークンはプルダウンで選んだ時点で即発行するように変更し、
  「コピー」ボタンはコピー処理だけを行うようにした（副作用を持たせない）

## 1.1.0

### 追加

- 構成同期タブに実行コマンドの生成フォームを追加。OS・対象サーバー・URL・APIトークンを
  選ぶだけでコマンドが組み上がる（URLはアクセス中のものが自動で入る）
- コマンド生成の対象サーバー欄から、その場で新規サーバーを登録できるようにした
- APIトークンは発行済みのものを選択でき、値が必要な場合はその場で新規発行して埋め込める
- Windows向けの検出スクリプト `scripts/hw_to_csv.ps1` を追加（CIM/WMIから収集、`-Push` 対応）
- システム名を **Dmidex** に統一
- バージョン表示を追加（画面右上・ログイン画面・`GET /api/version`）

### 修正

- 確認ダイアログでキャンセルしたとき、処理が完了しないまま止まることがあった。
  `dialog` の `close` イベントが発火しない環境があり、そこに依存していたため。
  閉じる処理を明示的に呼ぶ方式に変更した

## 1.0.0

初期リリース。

- パーツ・サーバーの登録と、サーバーへの割り当て／取り外し／移動
- 割り当て履歴とサーバーごとの現在構成の表示
- CSV一括登録（重複行の扱いを選択可能）とCSVエクスポート
- カテゴリ別の入力補助（メーカートグル・数値入力）
- ダッシュボード、列ソート、ページング、フィルタ
- ログイン認証、アカウント管理、APIトークン
- ゴミ箱（論理削除と復元）、操作履歴
- Proxmox/Linux向け検出スクリプトと構成同期
- 保証期限管理、写真添付
